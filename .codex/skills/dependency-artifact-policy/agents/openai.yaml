interface:
  display_name: "Dependency Artifact Policy"
  short_description: "Keep dependency artifacts package-owned"
  default_prompt: "Use $dependency-artifact-policy before changing dependencies, lockfiles, Node versions, or package-manager state."
