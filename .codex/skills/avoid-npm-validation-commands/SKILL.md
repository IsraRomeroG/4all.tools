---
name: avoid-npm-validation-commands
description: Use for code changes in this project. Never run npm run dev or npm run build unless the user explicitly asks for those commands.
---

# Avoid npm Validation Commands

Never run `npm run dev` or `npm run build` unless the user explicitly asks for those commands.
